#include<stdio.h>
#include<conio.h>
int main()
{
    int x,y,z;
    x=sizeof (int);
    y=sizeof (float);
    z=sizeof (double);
    printf("size of int=%d\nsize of float=%d\nsize of double=%d\n",x,y,z);
    getch();
    return 0;
} 
